#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    string number;
    cin >> number;
    char answer[number.length()];

    // generate_words();

    return 0;
}